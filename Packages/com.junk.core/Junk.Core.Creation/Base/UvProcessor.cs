using System.Collections.Generic;
using Unity.Mathematics;

namespace Junk.Core.Creation
{
    public static class UvProcessor
    {
        /*public List<int2> MergeCoordinates(int2[] array, float tolerance = 0.001f)
        {
            for (int i = 0; i < array.Length; i++){
                var curVert = array[i]; // get vertex [i]
                // compare it to the next vertices:
                for (int j = i+1; j < array.Length; j++){
                    // if any one inside limit distance...
                    if (math.distance(curVert, array[j]) < 0.0001f){
                        array[j] = curVert; // make it equal to vertex [i]
                    }
                }
            }
        }*/
    }
}